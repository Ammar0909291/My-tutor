/**
 * DNA replication scene — public module.
 *
 * No LLM parameter extraction: the figure is one fixed, authored replication
 * fork for bio.mol.dna-replication, so the pure half is the whole module. This
 * file exists only to keep the same pure/public split every generator in this
 * directory follows.
 */
export * from './dnaReplication.pure'
